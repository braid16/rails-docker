<h1> Edit a job </h1>

<%= simple_form_for [:admin, @job ] do |f| %>

<%= f.input :title %>
<%= f.input :description %>
<%= f.input :wage_lower_bound, :label =>"xzxx" %>
<%= f.input :wage_upper_bound, :label =>"xxsx" %>
<%= f.input :contact_email %>

  <%= f.submit "Submit" %>
<% end %>
